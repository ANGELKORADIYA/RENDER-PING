name: Render Ping

on:
  schedule:
    - cron: '*/5 11-23 * * *'
  workflow_dispatch:

jobs:
  ping-websites:
    runs-on: ubuntu-latest

    steps:
      - name: Ping websites using cURL
        run: |
          curl -I https://white-rock.onrender.com
          curl -I https://codeseveryware.onrender.com
